# Normalise line endings: stored as LF in the repository, native on checkout.
* text=auto

# Visual Studio solution files are conventionally CRLF.
*.sln text eol=crlf

# Binary assets (no line-ending conversion).
*.png binary
*.jpg binary
*.ico binary
*.snk binary
*.pfx binary
*.nupkg binary
*.snupkg binary
